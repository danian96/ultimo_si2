<%= form_with(model: asig_stock, local: true, html: {class: "form-horizontal"}) do |form| %>
  <% if asig_stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asig_stock.errors.count, "error") %> prohibited this asig_stock from being saved:</h2>

      <ul>
      <% asig_stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-6">
    <div class="form-group row">
      <%= form.label :fecha_de_asignacion,'Fecha de asignacion :' , class:"control-label text-right col-md-3" %>
      <div class="col-md-9">
        <%= form.date_select :asig_date %>
      </div>
    </div>
  </div>

  <div class="col-md-6">
    <div class="form-group row">
      <%= form.label :usuario , 'Usuario :', class:"control-label text-right col-md-3" %>
      <div class="col-md-9">
        <%= form.collection_select(:user_id, User.all, :id, :email, {hide_label: true},class: "form-control custom-select") %>
      </div>
    </div>
  </div>
    <div class="form-actions">
      <div class="row">
        <div class="col-md-6">
          <div class="row">
            <div class="col-md-offset-3 col-md-9">
              <%= form.submit "Guardar", class:"btn btn-info" %>
              <%= link_to 'Atras',asig_stocks_path , class:"btn btn-info"%>
            </div>
          </div>
        </div>
      </div>
    </div>
<% end %>
