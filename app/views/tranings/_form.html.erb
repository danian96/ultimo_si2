<%= form_with(model: traning, local: true) do |form| %>
  <% if traning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(traning.errors.count, "error") %> prohibited this traning from being saved:</h2>

      <ul>
      <% traning.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :init_date %>
    <%= form.date_select :init_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
